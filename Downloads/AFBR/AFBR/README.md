# AIGentAgent Omnichannel Sales Demo

## Prerequisites

- Node.js 18+
- MongoDB running locally (default URI: `mongodb://localhost:27017/aigentagent`)
- (Optional) Redis running on `localhost:6379` for faster sessions

## Setup

### 1. Backend

